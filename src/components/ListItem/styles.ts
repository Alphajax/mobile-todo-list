import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 50,
    paddingBottom: 8,
    paddingTop: 8,
    paddingLeft: 4,
    paddingRight: 4,
  },
  name: {
    fontSize: 18,
  },
});

export default styles;
