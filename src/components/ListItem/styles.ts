import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 55,
    paddingBottom: 10,
    paddingTop: 10,
    paddingLeft: 5,
    paddingRight: 5,
    flexDirection: 'row',
  },
  name: {
    color: '#293241',
    flex: 0.85,
    fontSize: 32,
  },
});

export default styles;
